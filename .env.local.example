DATABASE_URL=postgres://optometrist:optometrist@localhost:5432/optometrist
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Password reset emails. Leave unset to fall back to logging the reset
# link to the server console instead (useful for local dev without an
# API key). Get a key at https://resend.com -- free tier covers this
# app's volume with plenty of headroom.
RESEND_API_KEY=
